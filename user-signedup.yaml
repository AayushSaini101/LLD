oneOf:
  - name: UserSignedUp
    summary: A user has signed up
    payload:
      type: object
      required:
        - displayName
        - email
      properties:
        displayName:
          type: string
        email:
          type: string
          format: email
